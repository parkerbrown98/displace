# Browser-visible versioned API origin.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Server-component API origin. Override this when the web app runs in a container.
API_INTERNAL_URL=http://localhost:3001/api/v1

# Use `api` to connect public reads to the server. Defaults to deterministic fixtures.
WEB_DATA_SOURCE=fixture

# Use `api` to connect browser authentication and account settings to the server.
NEXT_PUBLIC_WEB_DATA_SOURCE=fixture
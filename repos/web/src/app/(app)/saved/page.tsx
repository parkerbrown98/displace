import { FeatureRoute } from "@/components/feature-route";

export default function SavedPage() {
  return <FeatureRoute activeNavigation="saved" eyebrow="Library" title="Saved" description="Your saved topics and posts." />;
}